import React from "react";

type ModalProps = {
  open: boolean;
};

function SearchLocation(props: ModalProps) {
  const {open} = props;
  return <div>SearchLocation</div>;
}

export default SearchLocation;
